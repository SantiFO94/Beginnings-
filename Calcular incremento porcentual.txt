#include <stdio.h>
int main(void) {
  
//Declaramos variables
    double growth;
    int population;
    double population2;
    int dpopulation2;
  
//Introducimos y asignamos valores
    scanf("%d", &population);
    scanf("%lf", &growth);
  
//Realizamos el cálculo del nuevo valor y eliminamos decimales
    population2 = population*((growth/100)+1);
    dpopulation2 = (int) population2;
  
//Mostramos el resultado
    printf("%d", dpopulation2);
    return 0;
}