#include <stdio.h>
int main(void) {
    int i, table;
    int number = 0;
    scanf("%d", &table);
    for(i = 0; i < 11 ; i++) {
        printf("%dx%d = %d\n", number, table, number*table);
        number += 1;
    }
    return 0;
}
